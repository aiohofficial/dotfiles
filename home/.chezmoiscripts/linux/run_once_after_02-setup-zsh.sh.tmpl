#!/usr/bin/env bash
set -eufo pipefail

sudo apt-get update
sudo apt-get install -y zsh
sudo chsh -s "$(command -v zsh)" "$(whoami)"

# Используем zplug (см. dot_zshrc) вместо Oh My Zsh - убираем, если ставили раньше.
rm -rf ~/.oh-my-zsh
